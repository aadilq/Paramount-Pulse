services:
    fastapi: ## service for the backend
        build: ./backend ## path to the backend folder containing our Dockerfile
        container_name: my-fastapi ## specify that the name of our container is my-fastapi
        ports:
          - "8000:8000" ## connects traffic from port 8000 on your physical machine (the host) to port 8000 inside your container
        env_file:
          - .env
        restart: unless-stopped
        depends_on:
            redis:
                condition: service_healthy
            elasticsearch:
                condition: service_healthy
    redis:
        image: redis:7.2
        container_name: my-redis
        ports:
          - "6379:6379"
        volumes:
          - redis-data:/data
        command: redis-server --requirepass ${REDIS_PASSWORD} --appendonly yes
        restart: unless-stopped
        healthcheck:
          test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
          interval: 10s
          timeout: 5s
          retries: 5
    elasticsearch:
        image: elasticsearch:8.13.0
        container_name: my-elasticsearch
        ports:
          - "9200:9200"
        volumes:
          - es-data:/usr/share/elasticsearch/data
        environment:
          - discovery.type=single-node
          - ELASTIC_PASSWORD=${ELASTIC_PASSWORD}
          - xpack.security.enabled=true
        restart: unless-stopped
        healthcheck: 
          test: ["CMD-SHELL", "curl -s -u elastic:${ELASTIC_PASSWORD} http://localhost:9200/_cluster/health | grep -q '\"status\":\"green\"\\|\"status\":\"yellow\"'"]
          interval: 20s
          timeout: 10s
          retries: 10
volumes:
    redis-data:
        driver: local
    es-data:
        driver: local


